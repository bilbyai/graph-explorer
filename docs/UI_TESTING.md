# UI Testing

Use `agent-browser` to run UI tests on the Graph Explorer. This tool allows you to automate interactions with the browser, making it easier to test the user interface.

Use the test account to log in to the Graph Explorer.
- **username**: `test@test.com`
- **password**: `testpassword`
