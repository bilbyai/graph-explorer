- [ ] When I press '3d' then go back to '2d' it fixes back the position (almost), but still looks like the ellipses are oblong not circle anymore. Might be because of broken perspective.
- [x] Proper loading when a graph is loading in. It's quite jank. Like it's showing the contents of the previous graph (i.e. the first one mounted), while the other one is loading. I needed clearer feedback that those are happening. Oh yeah and please don't show the initial placeholder graph becuase it looks weird after for example loading into 'X Entities' and I see some random "Vicon Holdings" graph idk where it came from (I hypothesize hardcoded)
